import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color background = Color(0xFF0B0E11);
  static const Color surface = Color(0xFF1E2329);
  
  // Secondary colors
  static const Color primary = Color(0xFF00FF41); // Neon Matrix Green
  static const Color secondary = Color(0xFFFF3B30); // Alert Red
  
  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF848E9C);
  
  // Accents
  static const Color accentBlue = Color(0xFF4A90E2);
  static const Color divider = Color(0xFF2B3139);
}
